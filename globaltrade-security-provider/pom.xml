<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.jiat.globaltrade</groupId>
        <artifactId>globaltrade-scm</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>globaltrade-security-provider</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>GlobalTrade Security Provider (Payara Custom JAAS Realm and LoginModule)</name>

    <dependencies>

        <!-- Jakarta EE API provided at runtime -->
        <dependency>
            <groupId>jakarta.platform</groupId>
            <artifactId>jakarta.jakartaee-api</artifactId>
            <version>${jakartaee.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- Payara Server Security Core APIs (GlassFish EE API) -->
        <dependency>
            <groupId>fish.payara.server.internal</groupId>
            <artifactId>glassfish-ee-api</artifactId>
            <version>6.2025.11</version>
            <scope>system</scope>
            <systemPath>${payara.home}/glassfish/modules/glassfish-ee-api.jar</systemPath>
        </dependency>

        <!-- Payara Server Security Implementation Core -->
        <dependency>
            <groupId>fish.payara.server.internal</groupId>
            <artifactId>security</artifactId>
            <version>6.2025.11</version>
            <scope>system</scope>
            <systemPath>${payara.home}/glassfish/modules/security.jar</systemPath>
        </dependency>

    </dependencies>

    <build>
        <finalName>globaltrade-security-provider</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <source>17</source>
                    <target>17</target>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.4.1</version>
            </plugin>
        </plugins>
    </build>

</project>
